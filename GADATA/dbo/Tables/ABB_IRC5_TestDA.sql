CREATE TABLE [dbo].[ABB_IRC5_TestDA] (
    [id]         INT           IDENTITY (1, 1) NOT NULL,
    [_value]     VARCHAR (128) NULL,
    [_timestamp] DATETIME      NULL,
    PRIMARY KEY CLUSTERED ([id] ASC)
);

