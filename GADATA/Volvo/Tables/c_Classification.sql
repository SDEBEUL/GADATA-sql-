CREATE TABLE [Volvo].[c_Classification] (
    [id]             INT        IDENTITY (1, 1) NOT NULL,
    [Classification] NCHAR (40) NULL,
    CONSTRAINT [PK_C_Classification] PRIMARY KEY CLUSTERED ([id] ASC)
);

